Local override libraries can be placed here if needed.

Default dependency resolution uses PlatformIO `lib_deps` from the root `platformio.ini`.
LVGL v8 is intentionally not versioned in this repository and is downloaded automatically.
